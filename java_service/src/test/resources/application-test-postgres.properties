# Testcontainers가 띄운 실제 PostgreSQL을 쓴다(@ServiceConnection이 접속 정보를
# 주입하므로 datasource 설정은 두지 않는다).
#
# 스키마는 Flyway가 만든다 — 운영과 같은 경로라 마이그레이션 SQL 자체가
# 검증된다. 그래서 ddl-auto는 검증만 한다.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
spring.session.jdbc.initialize-schema=never
