# Runtime dependencies for the FastAPI backend.
# Pulls in the ai/ inference stack (numpy, scipy, scikit-learn, xgboost, gmsh).
# matplotlib is included transitively but unused here (GUI-only in ai/visualization).
-r ../ai/requirements.txt

fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
python-dotenv>=1.0,<2
slowapi>=0.1.9,<1
