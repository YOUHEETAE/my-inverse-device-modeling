# Shared AI components

Common schemas, preprocessing metadata, model-version metadata, and utilities used
by more than one model family belong here.

`field_data.py` defines the generated Field Map container and scalar-display
selection shared by model inference, backend analysis, and frontend rendering.
