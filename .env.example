# Copy this file to .env and fill it in there. Nothing reads .env.example --
# it is committed to the repository, so a real key placed here would be
# published. .env is git-ignored and is the file Docker Compose loads.

# The AI provider. Any endpoint that speaks the OpenAI chat-completions API
# works, since that is all the client sends. Groq is the built-in default, so
# a key alone is enough to use it.
GROQ_API_KEY=replace_with_a_new_key

# Optional overrides; these defaults are already built into the application.
LLM_MODEL=openai/gpt-oss-120b
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_API_KEY_ENV=GROQ_API_KEY

# To use OpenAI directly instead, set all four of these and leave the Groq key
# unused. LLM_API_KEY_ENV names the variable the key is read from, so the key
# itself goes in whatever variable you name here.
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY_ENV=OPENAI_API_KEY
# OPENAI_API_KEY=replace_with_your_openai_key
# LLM_MODEL=replace_with_your_model

# Sampling temperature. Unset means 0.2, which keeps explanations consistent
# between runs. Leave the value EMPTY (just "LLM_TEMPERATURE=") to drop the
# parameter from the request entirely -- some models accept only their own
# default and answer 400 to anything else, and because a provider failure
# falls back to a local answer rather than an error, the only symptom is that
# every AI reply quietly stops coming from the model.
# LLM_TEMPERATURE=

# java_service only. How many LLM answers one account may spend per day,
# counted in Asia/Seoul. Unset means 40 -- roughly two long conversations,
# since a single thread caps at 20 turns. Predictions and comparisons are not
# counted; they run on our own models and cost nothing externally.
# DAILY_LLM_LIMIT=40

# Backend only (backend/app/main.py). Comma-separated list of origins the
# FastAPI CORS middleware will allow. Defaults to the local dev web ports if
# unset — override with the deployed frontend's real origin(s) in production.
CORS_ALLOW_ORIGINS=http://localhost:5174,http://127.0.0.1:5174

# java_service's PostgreSQL (accounts + sessions). Used by both the db
# container and java_service in docker-compose.yml. The service refuses to
# start without a password rather than falling back to a default, so a
# missing value fails loudly instead of silently running on a known one.
POSTGRES_DB=semiscope
POSTGRES_USER=semiscope
POSTGRES_PASSWORD=replace_with_a_local_password

# Google sign-in (java_service). Create an OAuth client at
# console.cloud.google.com and register the redirect URI Spring uses:
# <origin>/login/oauth2/code/google
GOOGLE_CLIENT_ID=replace_with_your_client_id
GOOGLE_CLIENT_SECRET=replace_with_your_client_secret

# java_service only. Where the browser goes after a successful login. Leave
# unset for the full compose stack — Caddy serves the frontend and the API
# from one origin, so the default "/" is correct. Set it to the Vite dev
# server's origin (http://localhost:5174) when you run java_service in Docker
# but the frontend on the host; otherwise "/" resolves to java_service itself,
# which has no root mapping and answers a 404 right after logging in.
# FRONTEND_URL=http://localhost:5174

# java_service only. Where Google returns the browser after sign-in.
#
# Leave unset locally — Spring builds it from the request and that is correct
# there. In a deployment it must be set, because Caddy only forwards /api/* to
# java_service and strips that prefix before doing so: Spring never sees /api,
# builds ".../login/oauth2/code/google", and Google's redirect then misses the
# /api/* route and lands on the static site instead of the service.
#
# Must match the redirect URI registered in the Google console exactly.
# OAUTH_REDIRECT_URI=https://yourdomain.example.com/api/login/oauth2/code/google

# docker-compose.yml only (the web/Caddy service). The domain Caddy requests
# a real Let's Encrypt certificate for. Leave unset for local `docker compose
# up` testing — Caddy then defaults to "localhost" and issues its own
# locally-trusted cert instead of calling Let's Encrypt.
SITE_DOMAIN=yourdomain.example.com
