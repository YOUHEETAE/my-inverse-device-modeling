# Precomputed long-channel MOSFET data

Run `generate_dataset.py` once to generate:

- the 0.5 µm Gmsh mesh;
- gate, oxide, and bulk region snapshots;
- ID–VG at VD = 0.05 V;
- `manifest.json` and compressed JSON results.

The Tkinter GUI only reads these saved results.
